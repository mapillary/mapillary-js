general:
  branches:
    only:
      - master
      - /feature-.*/
    ignore:
      - gh-pages

machine:
  node:
    version: 0.10.33

dependencies:
  pre:
    - npm install -g gulp
    - export DISPLAY=:99.0
    - sleep 5

test:
  override:
    - gulp tsd
    - gulp ts --env=TEST
    - gulp test