general:
  branches:
    ignore:
      - gh-pages

machine:
  node:
    version: 10.4.0

dependencies:
  cache_directories:
    - node_modules
  pre:
    - export DISPLAY=:99.0
  override:
    - npm install:
        timeout: 600

test:
  override:
    - npm test