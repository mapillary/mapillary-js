import {Alignment} from "../../../Viewer";

export interface IPopupOptions {
    clean?: boolean;
    float?: Alignment;
    offset?: number;
    opacity?: number;
    position?: Alignment;
}

export default IPopupOptions;
