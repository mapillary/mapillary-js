import {Float} from "../../../Viewer";

export interface IPopupOptions {
    clean?: boolean;
    float?: Float;
    offset?: number;
    opacity?: number;
    position?: Float;
}

export default IPopupOptions;
