/**
 * Interface for the options that define the behavior and
 * appearance of the outline create tag.
 *
 * @interface
 */
export interface OutlineCreateTagOptions {
    /**
     * Text color as hexadecimal number.
     * @default 0xFFFFFF
     */
    color?: number;
}
