export interface ILineTagParameters {
    icon?: string;
    interpolate?: boolean;
    lineColor?: number;
    lineWidth?: number;
    text?: string;
    textColor?: number;
}

export default ILineTagParameters;
